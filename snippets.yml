---
snippets:
  - name: Factory method
    acronym: fac
    body: |
      def $1
        @$1 ||= create(:$1:$2)
      end
  - name: One liner it
    acronym: oit
    body: |
      it { is_expected.to $1 }
